# Site specific configurations.

# that's where we will install most of the packages (ess, etc)
PREFIX=${HOME}/.emacs.d
#PREFIX=~/tmp/.emacs.d
# path where the installed emacs resides. that's to replace the org-mode shipped
# with emacs (should reside in ${APPPREFIX/lisp}).
#####
# Mac example:
APPPREFIX=/Applications/Emacs.app/Contents/Resources
APPLISPDIR=${APPPREFIX}/lisp
APPETCDIR=${APPPREFIX}/etc
APPDOCDIR=${APPPREFIX}/doc
APPINFODIR=${APPPREFIX}/info
#####
# Unix example:
#APPPREFIX=/usr/local/share/emacs/24.3
#APPLISPDIR=${APPPREFIX}/lisp
#APPETCDIR=${APPPREFIX}/lisp/etc
#APPDOCDIR=${APPPREFIX}/lisp/doc
#APPINFODIR=${APPPREFIX}/lisp/info

EMACS=emacs
ELCC=$(EMACS) -batch -q -no-site-file -no-init-file


# Site specific configurations.

# that's where we will install most of the packages (ess, etc)
PREFIX=${HOME}/.emacs.d
#PREFIX=~/tmp/.emacs.d
# path where the installed emacs resides. that's to replace the org-mode shipped
# with emacs (should reside in ${APPPREFIX/lisp}).
#####
# Mac example:
APPPREFIX=/Applications/Emacs.app/Contents/Resources
APPLISPDIR=${APPPREFIX}/lisp
APPETCDIR=${APPPREFIX}/etc
APPDOCDIR=${APPPREFIX}/doc
APPINFODIR=${APPPREFIX}/info
#####
# Unix example:
#APPPREFIX=/usr/local/share/emacs/24.3
#APPLISPDIR=${APPPREFIX}/lisp
#APPETCDIR=${APPPREFIX}/lisp/etc
#APPDOCDIR=${APPPREFIX}/lisp/doc
#APPINFODIR=${APPPREFIX}/lisp/info

##EMACS=emacs
EMACS=/Applications/Emacs.app/Contents/MacOS/Emacs
ELCC=$(EMACS) -batch -q -no-site-file -no-init-file

##Async org version
## Define the release that should be checked out and used.
## can also be master.
## Notes on releases:
## 8.0 works nicely with org-export, but does not provide org-sbe (it's called sbe
## 9.0: many changes were included, thus old org files might have problems. Check http://orgmode.org/Changes.html.
## 8.2.6 (04.2014)) contains org-sbe
org_release_async=release_8.3.6
## was release_8.3.2, then 8.3.4
#org_release=release_8.3.4
#org_release=release_8.3.6
org_release=release_9.0.1
########
## ESS
ess_release=v16.10
#ess_release=v16.04
#ess_release=master
